import React from "react";

const Loading = () => {
    return (
        <div className="mt-16 md:mt-40 flex justify-center items-center">
            <button className="btn loading">Loading</button>
        </div>
    );
};

export default Loading;
